# Changelog

All notable changes to `{{REPO_SLUG}}`.

## {{LAST_UPDATE}}

- Initial scaffold — repo created via `gh-repo-mirror`. Pages site ported from neo-brutalist template; skills and changelog arrays left empty so entries are added as each skill lands.
