---
name: jest-to-vitest-migration
description: Migrates a Jest test suite to Vitest, translating jest.config settings and mocks. Use when a session's task involves converting a project's test suite from Jest to Vitest.
---

# Jest to Vitest Migration

Synthetic fixture skill used only by session-to-skill-extractor dedup pre-screen tests.
It intentionally mirrors the vocabulary of the "jest-to-vitest-migration" candidate fixture
so the Jaccard token-overlap score against it clears the 0.3 pre-screen threshold.
